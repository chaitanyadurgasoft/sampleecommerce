apiVersion: apps/v1
kind: Deployment
metadata:
  name: login-service
  namespace: ecommerce
  labels:
    app: login-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: login-service
      tier: backend
  template:
    metadata:
      labels:
        app: login-service
        tier: backend
    spec:
      containers:
      - name: login-service
        image: chaitanyamanikumar/login-service:2.0.0
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "prod"
        - name: DB_SCHEMA
          value: "users_schema"
        - name: JAVA_OPTS
          value: "-Xmx512m -Xms256m"
        envFrom:
        - configMapRef:
            name: database-config
        - secretRef:
            name: database-secret
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 5
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 5
          timeoutSeconds: 3
      initContainers:
      - name: wait-for-postgres
        image: busybox:1.35
        command: ['sh', '-c', 'until nc -z postgres-service 5432; do echo waiting for postgres; sleep 2; done;']

---
apiVersion: v1
kind: Service
metadata:
  name: login-service
  namespace: ecommerce
  labels:
    app: login-service
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: login-service
    tier: backend
